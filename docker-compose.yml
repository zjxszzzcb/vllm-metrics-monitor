services:
  vmm:
    build: .
    container_name: vmm
    network_mode: host
    command: ["${METRICS_URL:-http://127.0.0.1:18800/metrics}", "-p", "8080"]
    volumes:
      - vmm-data:/root/.vmm
    restart: unless-stopped

volumes:
  vmm-data:
